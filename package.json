{
  "name": "sumity-monorepo",
  "version": "1.0.0",
  "description": "Monorepo script manager for Sumity Devotional Sticker Marketplace",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev:backend": "npm start --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "start": "npx -y concurrently --kill-others \"npm run dev:backend\" \"npm run dev:frontend\"",
    "test:downloads": "npm run test:downloads --prefix backend"
  }
}
